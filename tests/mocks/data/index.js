/**
 * Export all mock data
 */

const mockUsers = require('./users');
const mockCompanies = require('./companies');
const mockContacts = require('./contacts');
const mockDeals = require('./deals');
const mockActivities = require('./activities');

module.exports = {
  mockUsers,
  mockCompanies,
  mockContacts,
  mockDeals,
  mockActivities,
};
